[pytest]
# Fast unit tests live in tests/. Slow integration tests in tests/integration/
# are excluded by default — run explicitly with:
#   python tests/integration/test_full_app.py
# or:
#   pytest tests/integration/ --override-ini="testpaths=tests/integration"
testpaths = tests
norecursedirs = tests/integration
